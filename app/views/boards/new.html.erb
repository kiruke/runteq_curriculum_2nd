<div class="container">
  <div class="row">
    <div class="col-lg-8 offset-lg-2">
      <h1><%= t('.title') %></h1>
      <%# render partial: 'form', locals: { board: @board } %>
      <%= render partial: "shared/errors", locals: { object: @board } %>
      <%= render 'form', { board: @board } %>
    </div>
  </div>
</div>